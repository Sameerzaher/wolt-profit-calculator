/** Central route paths — use these instead of hard-coded strings. */
export const ROUTES = {
  home: "/",
  app: "/app",
  appAddShift: "/app/add-shift",
  appImportScreenshot: "/app/import-screenshot",
  appCourierInsights: "/app/courier-insights",
  appVehicleSettings: "/app/settings/vehicle",
  dashboard: "/dashboard"
} as const;
